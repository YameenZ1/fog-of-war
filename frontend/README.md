# Frontend - Historical Battle Analyzer

This `frontend` service is a React application built with Vite and styled using Tailwind CSS.

Its purpose is to provide a user interface for exploring and analyzing historical battles using AI-powered insights from the `agent` backend and contextual data from the `mcp-server` (Wikipedia wrapper).

At this stage, this folder only contains scaffolding and placeholder files. No real UI or business logic has been implemented yet.*** End Patch```} -->
