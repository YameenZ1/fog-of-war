# MCP Server - Wikipedia FastAPI Wrapper

The `mcp-server` service is a Python FastAPI microservice that wraps the Wikipedia API.

Its purpose is to provide a controlled, structured interface for fetching historical context and battle-related information from Wikipedia, which can then be consumed by the `agent` service.

Right now this folder just contains scaffolding and placeholder files. No real Wikipedia integration or request/response logic has been implemented yet.
